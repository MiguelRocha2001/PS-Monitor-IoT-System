gradlew clean
gradlew build   #ou gradlew build -x test to skip tests

cp -r build/ /home/miguel/Backend
cp -r sql/ /home/miguel/Backend
cp -r src/main/resources /home/miguel/Backend
cp Dockerfile /home/miguel/Backend
cp docker-compose.yml /home/miguel/Backend

docker compose up -d
docker compose down -v
docker-compose down --volumes




gradlew clean && gradlew build -x test && docker build --tag iot_system . && docker compose down -v && docker compose up -d